const {userDetails} = require("../../Db/DbCollections/collections");

module.exports.userLoginCredential = (data) =>{
    return new Promise(async(resolve,reject)=>{
        try {
            
        } catch (error) {
            
        }
    })
}